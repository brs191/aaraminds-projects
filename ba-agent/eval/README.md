# BA Agent Evaluation Placeholder

This directory is reserved for the local evaluation harness.

Phase 2 adds minimal local seed evaluation commands for `GTS-STANDUP` and `GTS-ROUTER`:

```bash
make eval-standup
make eval-router
```

`GTS-GATE` remains owned by Phase 3 gateway/control hardening.

Phase 3 adds:

```bash
make eval-gate
```

Phase 5 adds:

```bash
make eval-planning
make eval-retro
make eval-health
make eval-mvp
```
