# WhisperX STT sidecar dependencies.
# whisperx pulls in faster-whisper, ctranslate2, torch, torchaudio and
# pyannote.audio — expect a multi-GB install and a model download (~1.5GB for
# large-v3-turbo) on first run.
whisperx>=3.3.1,<4
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
