# Restricted Corpus Sample

This synthetic file belongs to a corpus that intentionally fails admission.

The evaluation harness must return `corpus_not_admitted` before retrieval and must not return this text as a search result.

